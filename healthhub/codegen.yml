schema: http://localhost:4000/graphql
documents: "src/**/*.graphql"
generates:
  src/app/gql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-angular
    config:
      addExplicitOverride: true
      serviceProvidedInRoot: true